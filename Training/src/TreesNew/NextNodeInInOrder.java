package TreesNew;
/*Given a node in a binary tree, please implement a function to retrieve its next node in the inorder
traversal sequence. There is a pointer to the parent node in each tree node.*/
public class NextNodeInInOrder {

	public static void main(String[] args)
	{
		Tree myTree = new Tree();
		myTree.insert(10);
		myTree.insert(8);
		myTree.insert(12);
		Node m = new Node(15, null, null);
		myTree.insert(6);
		myTree.insert(9);
		myTree.insert(11);
		//Incomplete..........
	}
}
